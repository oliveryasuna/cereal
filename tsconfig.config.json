{
  "extends": "./tsconfig.json",
  "include": [
    "./*.ts"
  ],
  "compilerOptions": {
    // Modules
    //--------------------------------------------------

    "module": "commonjs",
    "types": [
      "node"
    ],

    // Emit
    //--------------------------------------------------

    "noEmit": true
  }
}
